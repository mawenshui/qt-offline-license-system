# 安全策略

## 报告安全问题

不要在公开 Issue 中披露可利用细节、真实密钥、授权文件或客户数据。请通过 GitHub 仓库的 Security Advisories 私下报告，并包含：

- 受影响版本和平台。
- 最小复现步骤。
- 预期影响和已知缓解措施。
- 可公开时间建议。

## 支持范围

安全修复以最新 Release `v1.0.1` 为主。旧版本仅在问题可无风险回移时提供补丁。

## 密钥材料

仓库和 Release 永不包含生产签名私钥、密钥库、密钥库口令、客户授权文件或真实硬件请求。发现误提交时应立即撤销相关密钥、重写历史并发布安全公告；仅删除最新提交不足以消除泄漏。

本地审计日志只记录操作元数据、结果、输出路径和文件 SHA-256，并通过前序哈希形成完整性链；不得写入口令、私钥、产品解密密钥、原始硬件值或完整解密载荷。
